---
import { Toaster } from '@swal/ui';
interface Props { title?: string; }
const { title = 'OrionHealth — Web PWA' } = Astro.props;
---

<!doctype html>
<html lang="es">
  <head>
    <meta charset="UTF-8" /><meta name="viewport" content="width=device-width, initial-scale=1" />
    <title>{title}</title>
    <meta name="description" content="OrionHealth — santuario de salud personal, privado y offline-first (stack SWAL: Astro + Svelte 5 + @swal/ui + Cloudflare)" />
    <link rel="manifest" href="/manifest.json" /><meta name="theme-color" content="#06b6d4" />
  </head>
  <body>
    <slot />
    <Toaster client:load />
    <style is:global>
      @import '@swal/ui/tokens';
      html { background: var(--swal-bg); color: var(--swal-text); }
      body { margin: 0; font-family: var(--swal-font); -webkit-font-smoothing: antialiased; }
    </style>
  </body>
</html>
